# ============================================================
# RSSHub Reader 依赖说明
# ============================================================
# 以下包在 MoviePilot 运行时通常已随环境提供。
# 插件代码采用「可用即用、缺失降级」策略：无这些第三方库时，
# 自动回退到 Python 标准库（xml.etree / urllib / html.parser），
# 核心功能（含「规则通知」）仍可正常运行。
#
# 仅在需要更强容错/性能时才有必要安装：
feedparser>=6.0        # RSS/Atom 解析（降级：xml.etree，覆盖 RSS2.0 + Atom）
requests>=2.28         # HTTP 请求（降级：urllib.request）
beautifulsoup4>=4.12   # 正文图片提取（降级：html.parser）
